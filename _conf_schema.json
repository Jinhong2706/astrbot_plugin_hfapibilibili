{
  "quality": {
    "description": "视频画质选择",
    "hint": "720p 可直接下载；1080p 需要 ffmpeg 合并音视频流",
    "type": "string",
    "default": "720p",
    "options": ["720p", "1080p"]
  },
  "download_method": {
    "description": "下载方式",
    "hint": "aria2c 多线程下载（需要安装 aria2）；direct 普通下载。选择 aria2c 但未安装时将自动降级为 direct。",
    "type": "string",
    "default": "aria2c",
    "options": ["aria2c", "direct"]
  },
  "cache_dir": {
    "description": "视频缓存目录",
    "hint": "留空则使用系统临时目录",
    "type": "string",
    "default": ""
  },
  "temp_file_retention": {
    "description": "临时文件保留时间（秒）",
    "hint": "建议 300-3600，超时自动清理",
    "type": "int",
    "default": 600
  },
  "search_result_count": {
    "description": "搜索结果展示数量",
    "hint": "设置搜索时返回的视频数量，最大值限制为 50。默认 20。",
    "type": "int",
    "default": 20
  },
  "hot_count": {
    "description": "热门视频展示数量",
    "hint": "设置热门指令返回的视频数量，最大值限制为 50。默认 20。",
    "type": "int",
    "default": 20
  },
  "proxy": {
    "description": "HTTP 代理地址",
    "hint": "可选，如 http://127.0.0.1:7890",
    "type": "string",
    "default": ""
  },
  "api_base_url": {
    "description": "B站 API 服务地址",
    "hint": "默认使用 Hugging Face 托管服务，也可自行部署后修改",
    "type": "string",
    "default": "https://jinhong270-api.hf.space"
  },
  "custom_font_path": {
    "description": "自定义中文字体文件路径",
    "hint": "留空则自动检测系统字体。支持 ttf/ttc/otf 等格式。例如：C:/Windows/Fonts/simhei.ttf 或 /usr/share/fonts/noto/NotoSansCJK-Regular.ttc",
    "type": "string",
    "default": ""
  },
  "enable_search_image": {
    "description": "是否生成搜索图片",
    "hint": "启用后搜索结果会以图片形式发送（需要 Pillow）。关闭则使用纯文本列表。",
    "type": "bool",
    "default": true
  },
  "aria2_path": {
    "description": "aria2c 可执行文件路径",
    "hint": "留空则自动从系统 PATH 查找。例如：/usr/bin/aria2c 或 C:/Program Files/aria2/aria2c.exe",
    "type": "string",
    "default": ""
  },
  "enable_blacklist": {
    "description": "是否启用黑名单",
    "hint": "启用后黑名单中的用户无法使用本插件",
    "type": "bool",
    "default": false
  },
  "blacklist_ids": {
    "description": "黑名单用户ID",
    "hint": "每行一个用户ID，黑名单中的用户无法使用本插件的任何功能",
    "type": "text",
    "default": ""
  }
}
