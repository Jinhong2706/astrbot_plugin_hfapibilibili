# Changelog

## [2.0.4] - 2026-07-14

### Fixed
- 修复任意含 `bv` / `av` 片段的消息误触发下载的问题。

### Changed
- 自动下载仅在整条消息为完整 `https://www.bilibili.com/video/...` 链接时触发。
- 链接中的视频号须为 `BV1` 开头，或 `av11` 开头的 AV 号。
- 单独发送 BV 号 / AV 号不再触发下载。
- 消息中夹杂其他文字时不再触发自动下载。

## [2.0.3] - 2026-07-14

### Added
- 黑名单功能：新增 `enable_blacklist` 和 `blacklist_ids` 配置项，支持通过用户 ID 禁止指定用户使用插件。
- 停止点播时自动清理对应用户已下载的封面图片。

### Fixed
- 修复用户会话隔离问题：当用户 A 进入点播模式后，其他用户的消息不再错误地传入用户 A 的搜索词。

### Changed
- 会话标识改为按用户隔离（`unified_msg_origin` + `sender_id`），每个用户的点播会话相互独立。

## [2.0.2] - 2026-07-10

### Added
- 异步重试工具 `with_retry`：API 请求、视频下载和封面下载均支持自动重试（默认 5 次，间隔 5 秒），减少临时网络波动导致的失败。
- 下载失败时自动降级方案：aria2 失败后自动切换为普通下载。

### Fixed
- 修复插件各处静默吞错的问题：API 请求失败、下载失败、搜索失败等现在会向用户返回明确的错误提示，而非悄悄返回空。
- 修复视频下载失败的多种边界情况（文件缺失、大小异常等）被正确捕获并反馈。
- 修复代理配置在某些请求中未正确传递的问题。

### Changed
- 重构 `main.py`：提取公共函数 `_download_video`、`_show_video_results`、`_extract_video_list`、`_format_video_list_text`，消除大量重复代码。
- 统一视频列表提取逻辑，兼容 API 返回的多种 JSON 结构。
- 代码格式化与空行清理，提高可读性。

## [2.0.1] - 2026-07-01

### Added
- 下载方式配置 `download_method`：可选 `aria2c`（多线程下载）或 `direct`（普通下载）。
- 当选择 aria2 但系统中未安装时，自动降级为普通下载，不再直接报错不可用。
- 封面下载也支持 `download_method` 配置，与视频下载保持一致。

### Changed
- `display_name` 由 "B站点播" 改为 "Bilibili"。

## [2.0.0] - 2026-07-01

### Added
- 升级至 v2.0.0：适配自托管 Bilibili API，提升解析稳定性。
- 新增 `b站热门` / `B站热门` 指令，展示当前热门视频列表。
- 新增 `hot_count` 配置项，控制热门视频展示数量。
- 搜索指令改为 `b站点播 <关键词>`，支持直接带关键词搜索或进入交互式点播会话。
- 新增 `停止点播` 指令，退出点播会话。
- `api_base_url` 默认地址更新为新的托管域名。

### Fixed
- 修复 AV 号下载时画质参数未正确传递的问题（1080p 和 720p 现在对 AV 号也生效）。

### Changed
- `_conf_schema.json` 中 `api_base_url` 的 hint 更新为"也可自行部署后修改"。
- 版本号从 1.8.0 升至 2.0.0，标记一次大规模功能迭代。
- 内部 API 端点路径调整（`/bilibili/video/aid/` 替代 `/bilibili/video/avid/`）。

## [1.8.0] - 2026-05-29

### Added
- 插件模块拆分：将单体 `main.py` 拆分为 `core/`（API、会话、视频处理）、`assets/`（图片生成）、`utils/`（工具函数）、`config.py`。
- 新增配置项 `custom_font_path`：允许用户手动指定中文字体文件路径。
- 新增配置项 `enable_search_image`：可关闭搜索图片生成，改用纯文本列表。

### Fixed
- 修复多处致命异常未被捕获导致插件崩溃的问题（视频下载、ffmpeg 合并、文件发送等）。
- 修复导入路径问题：模块拆分后统一使用相对导入。
- 修复 `__init__.py` 文件名错误（`_init_.py` 重命名为 `__init__.py`）。
- 修复 1080p Dash 流音频变量名错误导致的判断失效。
- 修复视频发送失败时静默丢弃错误的问题。

### Changed
- 指令名称本地化：`search` → `b站点播`，`searchstop` → `停止点播`。
- 点播提示语优化：「告诉我你的关键词吧～」→「告诉我你想点播的关键词吧～」。
- ffmpeg 缺失时添加明确警告提示。
- 搜索图片生成逻辑移至独立模块 `assets/image_generator.py`。

## [1.6.0] - 2026-05-16

### Added
- 版本号从 1.5.0 提升至 1.6.0，标志着插件功能的阶段性稳定。
- 搜索结果数量可通过 `search_result_count` 配置限制（默认 20，最大 50）。

### Changed
- README 文档大幅度更新，补充安装说明、ffmpeg 依赖说明、常见问题等。

## [1.5.0] - 2026-05-09

### Added
- 视频搜索功能：支持 `search <关键词>` 指令，返回搜索结果列表。
- 搜索结果图片卡片生成：安装 Pillow 后，搜索结果以图文卡片形式展示（含封面、标题、UP 主、播放量、时长）。
- b23.tv 短链接解析与还原。
- 点播交互式会话：搜索后回复序号即可下载对应视频，会话超时自动清理。
- Dash 音视频流分离下载与 ffmpeg 合并（支持 720p 和 1080p）。
- 临时文件自动清理（按 `temp_file_retention` 配置周期清理）。
- HTTP 代理支持（`proxy` 配置）。
- 中文字体自动检测，跨平台适配。

### Fixed
- 修复多处视频下载失败未正确反馈的问题。

## [1.1.3] - 2026-05-01

### Added
- 项目初始化，创建插件仓库。
- 基础 B 站视频链接解析与下载功能（支持 BV 号和完整链接）。
- 基本配置框架（画质选择、缓存目录）。
- metadata.yaml 配置。
